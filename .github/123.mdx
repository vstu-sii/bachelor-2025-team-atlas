<Mermaid
  chart="
    C4Context
      title C4: Контекстная диаграмма

      Person(user, 'Пользователь', 'Использует систему')
      System(systemA, 'System A', 'Основная система')
      System_Ext(extSystem, 'Payment Gateway', 'Внешняя система')

      Rel(user, systemA, 'Использует')
      Rel(systemA, extSystem, 'Отправляет запрос', 'HTTPS')
  "
/>
